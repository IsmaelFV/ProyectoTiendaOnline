---
export const prerender = false;

// Login admin eliminado - se usa el login unificado /auth/login
// Si el usuario es admin, será redirigido automáticamente a /internal-admin
return Astro.redirect('/auth/login');
---
